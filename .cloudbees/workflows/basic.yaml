apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: My automation
on:
  push:
    branches:
      - "**"
jobs:
  build:
    steps:
      - uses: docker://cgr.dev/chainguard/go:latest
        name: build
        kind: build
        shell: sh
        run: |
          echo "build"
  test:
    steps:
      - uses: docker://cgr.dev/chainguard/go:latest
        kind: test
        name: test
        shell: sh
        run: |
          echo "test"
    needs:
      - build
  scan:
    steps:
      - uses: docker://cgr.dev/chainguard/go:latest
        name: scan
        kind: scan
        shell: sh
        run: |
          echo "scan"
    needs:
      - test
  deploy:
    steps:
      - uses: docker://cgr.dev/chainguard/go:latest
        kind: deploy
        name: deploy
        shell: sh
        run: |
          echo "deploy"
    needs:
      - scan
